let graphqlApiBaseUrl = 'https://graphql.orleans-tech.com'

/*
const isBrowser = typeof window !== 'undefined'

if (isBrowser && window.location.hostname == 'localhost') {
  graphqlApiBaseUrl = 'http://localhost:3000'
}
*/

export const GRAPHQL_API_BASE_URL = graphqlApiBaseUrl
